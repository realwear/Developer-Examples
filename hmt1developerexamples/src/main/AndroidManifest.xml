<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.realwear.hmt1developerexamples">

    <uses-permission android:name="android.permission.RECORD_AUDIO"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".ActionButtonActivity" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".DocumentActivity" />
        <activity android:name=".MovieActivity" />
        <activity android:name=".BarcodeActivity" />
        <activity android:name=".TTSActivity" />
        <activity android:name=".SpeechRecognizerActivity" />
        <activity android:name=".DictationActivity" />
        <activity android:name=".AudioCaptureActivity" />
        <activity android:name=".MicrophoneReleaseActivity" />
        <activity android:name=".ShowHelpActivity" />
        <activity android:name=".BNFGrammarActivity" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>